/**
 * special modifiers for realms
 * INSTANT: better at instant spells
 * CONTINUOUS: better at continuous spells
 * LOUD: makes noise when casting
 * INNATE: spells are not learned from books
 * HP_CAST: spells are cast with hp rather than mana
 */

RLM_SPCL(INSTANT)
RLM_SPCL(CONTINUOUS)
RLM_SPCL(LOUD)
RLM_SPCL(INNATE)
RLM_SPCL(HP_CAST)
